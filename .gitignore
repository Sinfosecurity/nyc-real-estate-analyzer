node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vite
coverage
# GitHub rejects workflow files unless the token has the `workflow` scope.
# Keep CI/Pages workflow YAML local until that scope is available.
.github/workflows/
