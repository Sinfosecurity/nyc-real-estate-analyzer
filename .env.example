# Optional. Local development uses Vite defaults. No secrets are required.
# BASE_PATH is a build-time public path prefix for GitHub Pages project sites.
# Leave unset for localhost and most custom domains.

# PUBLIC CLIENT CONFIGURATION
# BASE_PATH=/nyc-real-estate-analyzer/

# SERVER-ONLY SECRET
# None. NYC GeoSearch and SODA are public, keyless endpoints.

# OPTIONAL DEVELOPMENT CONFIGURATION
# VITE_DEV_PORT=5173
